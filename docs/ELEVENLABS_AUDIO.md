# ElevenLabs audio module

Optional text-to-speech and speech-to-text support for approved briefings and authorised recordings.

The feature is disabled by default and must be configured through server-side environment variables. Credentials must never be committed to the repository or exposed to browser code.
